[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.install]
cmds = ["mvn dependency:go-offline -B"]

[phases.build]
cmds = ["mvn clean package -DskipTests -B -Dmaven.test.skip=true -Dproject.build.directory=./target"]

[start]
cmd = "java -jar target/demoWeb-0.0.1-SNAPSHOT.jar" 