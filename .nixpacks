[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.install]
cmds = ["mvn dependency:go-offline"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[phases.start]
cmds = ["java -jar target/demoWeb-0.0.1-SNAPSHOT.jar"]

[start]
cmd = "java -jar target/demoWeb-0.0.1-SNAPSHOT.jar" 